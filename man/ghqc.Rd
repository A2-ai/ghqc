% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ghqc.R
\name{ghqc}
\alias{ghqc}
\title{Run the ghqc UI as a background job}
\usage{
ghqc(
  directory = here::here(),
  port = NULL,
  config_dir = NULL,
  log_level = Sys.getenv("GHQC_LOG_LEVEL", "TRACE")
)
}
\arguments{
\item{directory}{Path to the project directory. Defaults to the project root
as determined by \code{\link[here:here]{here::here()}}.}

\item{port}{Integer port to bind the server to. If \code{NULL} (default), a
random available port is selected automatically.}

\item{config_dir}{Path to the ghqc configuration directory. If \code{NULL}
(default), ghqc uses its default configuration discovery logic.}

\item{log_level}{Level of logging the server runs with. Recommended to leave as TRACE
since \code{\link[=ghqc_log]{ghqc_log()}} will filter levels.}
}
\value{
Called for its side effect of starting the server and opening the
browser. Returns \code{NULL} invisibly.
}
\description{
Starts the ghqc web UI as a supervised background R process and opens it in
the browser. Any previously running ghqc server is stopped first. Use
\code{\link[=ghqc_stop]{ghqc_stop()}} to stop the server, \code{\link[=ghqc_status]{ghqc_status()}} to check its status, or
\code{\link[=ghqc_reconnect]{ghqc_reconnect()}} to reopen the browser tab without restarting the server.
}
\examples{
\dontrun{
# Start with defaults (project root, random port)
ghqc()

# Start on a specific port
ghqc(port = 8080)

# Start for a subdirectory with a custom config location
ghqc(directory = "analysis", config_dir = "~/.config/ghqc")
}

}
