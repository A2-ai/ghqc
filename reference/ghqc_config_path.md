# The default install location for the ghqc custom configuration repository

The default install location for the ghqc custom configuration
repository

## Usage

``` r
ghqc_config_path()
```

## Value

string containing the default path to the ghqc custom configuration
repository (~/.local/share/ghqc/\<config repo name here\>
